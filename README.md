TODO:
- use t.Helper() and t.Cleanup() functions
- prepare proper makefiles
- control root user on containers
- unify go folder logic across services
- using a global DB is the good approach? we have the nil checks everywhere